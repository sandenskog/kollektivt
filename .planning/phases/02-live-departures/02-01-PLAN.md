---
phase: 02-live-departures
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/index.ts
  - src/services/sl-departures.ts
  - src/services/sl-departures.test.ts
  - src/hooks/useDepartures.ts
  - src/hooks/usePersistedStops.ts
autonomous: true
requirements: [DEPT-01, DEPT-02, DEPT-03, DEPT-04, DEPT-05, DEPT-06]

must_haves:
  truths:
    - "fetchDepartures returns departures with line, direction, scheduled/expected times, state, and deviations"
    - "useDepartures polls every 30 seconds and keeps last successful data on error"
    - "Countdown is computed locally from expected timestamp, not from API display field"
    - "PersistedData schema extended to v2 with filters field"
  artifacts:
    - path: "src/types/index.ts"
      provides: "Departure, StopDeviation, DepartureResponse, StopFilters types"
      contains: "interface Departure"
    - path: "src/services/sl-departures.ts"
      provides: "fetchDepartures function and getCountdown helper"
      exports: ["fetchDepartures", "getCountdown"]
    - path: "src/services/sl-departures.test.ts"
      provides: "Unit tests for departure service"
      contains: "describe"
    - path: "src/hooks/useDepartures.ts"
      provides: "Polling hook returning departuresByStop, errors, loading, refresh"
      exports: ["useDepartures"]
    - path: "src/hooks/usePersistedStops.ts"
      provides: "Extended with filters support and schema migration to v2"
      exports: ["usePersistedStops"]
  key_links:
    - from: "src/hooks/useDepartures.ts"
      to: "src/services/sl-departures.ts"
      via: "import fetchDepartures"
      pattern: "fetchDepartures"
    - from: "src/hooks/useDepartures.ts"
      to: "src/types/index.ts"
      via: "import DepartureResponse"
      pattern: "DepartureResponse"
    - from: "src/hooks/usePersistedStops.ts"
      to: "src/types/index.ts"
      via: "import StopFilters, PersistedData"
      pattern: "StopFilters"
---

<objective>
Build the departure data layer: types, API service, polling hook, and persistence schema extension.

Purpose: Create all data contracts and logic needed before UI components can render departures.
Output: Departure types, fetch service with tests, polling hook, extended localStorage schema.
</objective>

<execution_context>
@/Users/richardsandenskog/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardsandenskog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-live-departures/02-RESEARCH.md

<interfaces>
<!-- Existing types and patterns the executor needs -->

From src/types/index.ts:
```typescript
export interface SLSite {
  id: number;
  name: string;
  lat: number;
  lon: number;
  stop_areas?: { id: number; name: string }[];
}

export interface SelectedStop {
  id: number;
  name: string;
  lat: number;
  lon: number;
}

export interface PersistedData {
  version: number;
  selectedStops: SelectedStop[];
}
```

From src/services/sl-sites.ts (pattern to follow):
```typescript
export async function loadSites(): Promise<SLSite[]> {
  if (sitesCache) return sitesCache;
  const res = await fetch('https://transport.integration.sl.se/v1/sites?expand=true');
  if (!res.ok) throw new Error(`SL API error: ${res.status} ${res.statusText}`);
  sitesCache = await res.json();
  return sitesCache!;
}
```

From src/hooks/usePersistedStops.ts:
```typescript
const STORAGE_KEY = 'kollektivt_data';
const SCHEMA_VERSION = 1;

function migrate(data: unknown): PersistedData { /* ... */ }
function loadFromStorage(): PersistedData { /* ... */ }
function saveToStorage(data: PersistedData): void { /* ... */ }

export function usePersistedStops() {
  // Returns { selectedStops, toggleStop, isSelected }
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define departure types and build service with tests</name>
  <files>src/types/index.ts, src/services/sl-departures.ts, src/services/sl-departures.test.ts</files>
  <behavior>
    - fetchDepartures(siteId) calls correct URL and returns typed DepartureResponse
    - fetchDepartures throws on non-OK HTTP response
    - getCountdown returns "Now" when expected time is in the past or within 0 minutes
    - getCountdown returns "3 min" for a departure 3 minutes from now
    - getCountdown returns "14:23" format for departures 60+ minutes away
    - getCountdown handles the timezone-less API timestamps correctly (append +01:00 CET offset before parsing, or parse HH:MM as strings)
  </behavior>
  <action>
    1. Extend src/types/index.ts with departure types (do NOT remove existing types):
       - `Departure` interface matching the verified API response shape from RESEARCH.md (destination, direction_code, direction, state, display, scheduled, expected, journey, stop_area, stop_point, line with transport_mode, deviations array)
       - `StopDeviation` interface (id, importance_level, message, scope with stop_areas and lines)
       - `DepartureResponse` interface (departures: Departure[], stop_deviations: StopDeviation[])
       - `StopFilters` interface (directions?: number[], lines?: number[])
       - Add `filters?: Record<number, StopFilters>` to PersistedData

    2. Create src/services/sl-departures.ts following sl-sites.ts pattern:
       - `fetchDepartures(siteId: number): Promise<DepartureResponse>` fetching from `https://transport.integration.sl.se/v1/sites/${siteId}/departures?forecast=60`
       - `getCountdown(departure: Departure): string` that computes countdown from expected timestamp vs current time
         - IMPORTANT: API timestamps lack timezone suffix (e.g. "2026-03-06T14:23:12"). Parse hours and minutes as strings from the timestamp to avoid timezone ambiguity. Compare with current local time.
         - Returns "Now" for <= 0 min, "{N} min" for 1-59 min, "HH:MM" for 60+ min
       - `getAbsoluteTime(departure: Departure): string` that extracts HH:MM from expected timestamp (string parsing, not Date)
       - `getDelayMinutes(departure: Departure): number` computing difference between expected and scheduled in minutes

    3. Create src/services/sl-departures.test.ts with vitest:
       - Mock global fetch for fetchDepartures tests
       - Test getCountdown with frozen time (vi.useFakeTimers) for "Now", "3 min", and "14:23" cases
       - Test getAbsoluteTime returns correct HH:MM
       - Test getDelayMinutes returns 0 for on-time, positive for delayed
       - Test fetchDepartures throws on non-OK response
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/kollektivt && npx vitest run src/services/sl-departures.test.ts</automated>
  </verify>
  <done>All departure service tests pass. Types exported. getCountdown handles timezone-less timestamps correctly.</done>
</task>

<task type="auto">
  <name>Task 2: Build polling hook and extend persistence</name>
  <files>src/hooks/useDepartures.ts, src/hooks/usePersistedStops.ts</files>
  <action>
    1. Create src/hooks/useDepartures.ts:
       - `useDepartures(stops: SelectedStop[])` hook that:
         - Maintains `departuresByStop` as Map&lt;number, DepartureResponse&gt; state
         - Maintains `errors` as Map&lt;number, boolean&gt; (true = last fetch for this stop failed)
         - Maintains `loading` boolean (true only on initial fetch, not on refreshes)
         - `fetchAll` callback using Promise.allSettled to fetch all stops in parallel
         - On fulfilled: update departuresByStop for that stop, clear error flag
         - On rejected: set error flag for that stop, KEEP previous departuresByStop data (stale-while-revalidate)
         - useEffect with setInterval(fetchAll, 30_000) + cleanup clearInterval
         - useCallback for fetchAll with stops as dependency (restarts interval when stops change)
         - Expose `refresh` function that calls fetchAll (for manual refresh button)
         - Maintain a `tick` counter state incremented every 60 seconds via separate setInterval, to trigger re-renders for countdown ticking (countdowns recomputed at render time using getCountdown)
       - Returns { departuresByStop, errors, loading, refresh, tick }

    2. Extend src/hooks/usePersistedStops.ts:
       - Bump SCHEMA_VERSION to 2
       - Update migrate() to handle v1 -> v2 migration (add empty filters: {} if missing)
       - Add `filters` to state (from PersistedData)
       - Add `setFilters(stopId: number, filters: StopFilters)` callback that updates and persists
       - Add `getFilters(stopId: number): StopFilters` that returns filters for a stop (default: empty = show all)
       - Clean up filters for stops no longer in selectedStops when toggleStop removes a stop
       - Return additionally: { filters, setFilters, getFilters }
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/kollektivt && npx vitest run</automated>
  </verify>
  <done>useDepartures hook polls every 30s, handles errors gracefully, ticks countdown every 60s. usePersistedStops extended with filter persistence and v1->v2 migration. All existing tests still pass.</done>
</task>

</tasks>

<verification>
- `npx vitest run` -- all tests pass (existing + new departure service tests)
- `npx tsc --noEmit` -- no TypeScript errors
- Types are exported and importable from src/types/index.ts
</verification>

<success_criteria>
- Departure types fully defined matching SL Transport API response shape
- fetchDepartures fetches from correct endpoint and returns typed data
- getCountdown computes correct countdown from timestamps without timezone issues
- useDepartures polls all selected stops in parallel every 30s
- Error handling keeps last successful data (stale-while-revalidate)
- usePersistedStops extended with filter get/set and schema migration
- All tests green
</success_criteria>

<output>
After completion, create `.planning/phases/02-live-departures/02-01-SUMMARY.md`
</output>
