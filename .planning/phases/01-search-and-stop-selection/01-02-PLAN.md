---
phase: 01-search-and-stop-selection
plan: 02
type: execute
wave: 2
depends_on: ["01-01"]
files_modified:
  - src/App.tsx
  - src/App.css
  - src/components/SearchBar.tsx
  - src/components/StopList.tsx
  - src/components/StopItem.tsx
  - src/components/MyStops.tsx
autonomous: false
requirements:
  - SRCH-01
  - SRCH-02
  - SRCH-03
  - SRCH-04
  - SRCH-05

must_haves:
  truths:
    - "User can type an address and see autocomplete suggestions from Nominatim in a dropdown"
    - "User can click a suggestion and see nearby stops sorted by distance"
    - "User can tap 'Use my location' and get nearby stops without typing"
    - "User can tap a stop to select it, and it appears in 'My stops'"
    - "User can deselect a stop from 'My stops' to remove it"
    - "Selections survive a full page reload"
    - "First-time user sees prominent search field with instruction text"
    - "Returning user with saved stops sees 'My stops' immediately"
  artifacts:
    - path: "src/components/SearchBar.tsx"
      provides: "Address input with autocomplete dropdown and location button"
      min_lines: 40
    - path: "src/components/StopList.tsx"
      provides: "Nearby stops results list"
      min_lines: 20
    - path: "src/components/StopItem.tsx"
      provides: "Single stop row with name, distance, select toggle"
      min_lines: 15
    - path: "src/components/MyStops.tsx"
      provides: "Selected stops section"
      min_lines: 20
    - path: "src/App.tsx"
      provides: "Main app wiring all components together"
      min_lines: 40
  key_links:
    - from: "src/components/SearchBar.tsx"
      to: "src/hooks/useNominatim.ts"
      via: "useNominatim hook for debounced search"
      pattern: "useNominatim"
    - from: "src/components/SearchBar.tsx"
      to: "src/hooks/useGeolocation.ts"
      via: "useGeolocation hook for location button"
      pattern: "useGeolocation"
    - from: "src/App.tsx"
      to: "src/hooks/useNearbyStops.ts"
      via: "useNearbyStops hook with coordinates from search/geolocation"
      pattern: "useNearbyStops"
    - from: "src/App.tsx"
      to: "src/hooks/usePersistedStops.ts"
      via: "usePersistedStops for selection state and persistence"
      pattern: "usePersistedStops"
    - from: "src/components/StopItem.tsx"
      to: "src/hooks/usePersistedStops.ts"
      via: "toggleStop callback passed as prop"
      pattern: "toggleStop|onToggle"
---

<objective>
Build all UI components and wire them to the data layer from Plan 01: SearchBar with Nominatim autocomplete and geolocation button, StopList/StopItem for nearby stop results, MyStops for selected stops, and App.tsx orchestrating the full flow.

Purpose: Deliver the complete Phase 1 user experience — find stops by address or location, select them, and have selections persist.
Output: Working app where users can search, select stops, and reload without losing selections.
</objective>

<execution_context>
@/Users/richardsandenskog/.claude/get-shit-done/workflows/execute-plan.md
@/Users/richardsandenskog/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-search-and-stop-selection/01-CONTEXT.md
@.planning/phases/01-search-and-stop-selection/01-RESEARCH.md
@.planning/phases/01-search-and-stop-selection/01-01-SUMMARY.md

<interfaces>
<!-- Types and hooks from Plan 01 that this plan consumes -->

From src/types/index.ts:
```typescript
export interface SLSite {
  id: number;
  name: string;
  lat: number;
  lon: number;
  stop_areas?: { id: number; name: string }[];
}

export interface NominatimResult {
  display_name: string;
  lat: string;
  lon: string;
}

export interface NearbyStop extends SLSite {
  distance: number;
}

export interface SelectedStop {
  id: number;
  name: string;
  lat: number;
  lon: number;
}
```

From src/hooks/useNominatim.ts:
```typescript
export function useNominatim(query: string, debounceMs?: number): {
  results: NominatimResult[];
  loading: boolean;
}
```

From src/hooks/useGeolocation.ts:
```typescript
export function useGeolocation(): {
  lat: number | null;
  lon: number | null;
  loading: boolean;
  error: string | null;
  requestLocation: () => void;
}
```

From src/hooks/useNearbyStops.ts:
```typescript
export function useNearbyStops(lat: number | null, lon: number | null): {
  stops: NearbyStop[];
  loading: boolean;
  error: string | null;
}
```

From src/hooks/usePersistedStops.ts:
```typescript
export function usePersistedStops(): {
  selectedStops: SelectedStop[];
  toggleStop: (stop: SelectedStop) => void;
  isSelected: (id: number) => boolean;
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build UI components</name>
  <files>src/components/SearchBar.tsx, src/components/StopList.tsx, src/components/StopItem.tsx, src/components/MyStops.tsx</files>
  <action>
1. Create `src/components/SearchBar.tsx`:
   - Text input for address search, always visible at top
   - Uses `useNominatim` hook internally for debounced search
   - Renders dropdown below input showing NominatimResult suggestions (display_name)
   - Clicking a suggestion calls `onSelectAddress(lat: number, lon: number)` prop
   - "Use my location" button next to input — calls `onRequestLocation()` prop
   - Shows loading spinner/text while Nominatim is fetching
   - Dropdown closes when a suggestion is selected or user clicks outside
   - Minimum visual styling with Tailwind-free approach (plain CSS in App.css or inline) — remember design is "for placement only"

2. Create `src/components/StopItem.tsx`:
   - Props: `stop: NearbyStop | SelectedStop`, `isSelected: boolean`, `onToggle: () => void`, `showDistance?: boolean`
   - Displays: stop name, distance formatted as "XXXm" (if showDistance), selected state highlight
   - Clicking the row calls onToggle (select/deselect toggle)
   - Selected stops get a visual highlight (e.g. light background or border)
   - Transport mode icons deferred — show stop name and distance only for now (icons will come with Phase 2 departure data, per research recommendation)

3. Create `src/components/StopList.tsx`:
   - Props: `stops: NearbyStop[]`, `isSelected: (id: number) => boolean`, `onToggleStop: (stop: SelectedStop) => void`, `loading: boolean`
   - Maps over stops, renders StopItem for each
   - Shows loading state while stops are being fetched
   - Shows "No stops found nearby" message if stops array is empty and not loading
   - Heading: "Nearby stops"

4. Create `src/components/MyStops.tsx`:
   - Props: `stops: SelectedStop[]`, `onRemoveStop: (stop: SelectedStop) => void`
   - Renders selected stops with remove capability (clicking deselects)
   - Only renders if stops.length > 0
   - Heading: "My stops"
   - This section is the anchor point for Phase 2 departure display
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/kollektivt && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>All four components created, properly typed, and build succeeds without TypeScript errors</done>
</task>

<task type="auto">
  <name>Task 2: Wire App.tsx and style the layout</name>
  <files>src/App.tsx, src/App.css</files>
  <action>
1. Rewrite `src/App.tsx` to orchestrate the full flow:
   - State: `coordinates: { lat: number; lon: number } | null` — set by search or geolocation
   - Use hooks: `usePersistedStops()`, `useNearbyStops(coordinates?.lat, coordinates?.lon)`, `useGeolocation()`
   - Layout order:
     a. App title "Kollektivt" as heading
     b. `<MyStops>` section (only if selectedStops.length > 0)
     c. `<SearchBar>` — onSelectAddress sets coordinates, onRequestLocation triggers geolocation
     d. Instruction text for first-time users: "Search for an address or use your location to find nearby stops" — only shown when no selected stops AND no search results
     e. `<StopList>` — shown when coordinates are set (search or geolocation result active)
   - When geolocation returns lat/lon, update coordinates state to trigger nearby stops
   - SearchBar stays visible at all times (per locked decision)
   - Returning user flow: load persisted stops on mount, show MyStops immediately, search is available but secondary

2. Create `src/App.css` with minimal "for placement only" styling:
   - Max-width container (e.g. 600px), centered
   - Basic spacing between sections
   - Light background, clean typography (system fonts or Nunito from existing app)
   - Input field styling: full width, basic padding/border
   - Dropdown: positioned below input, basic white background with border
   - Stop items: basic list with clickable rows, subtle hover state
   - Selected state: light highlight background
   - Keep it minimal — Phase 3 handles responsive design polish

3. Ensure the full flow works end-to-end:
   - Type address -> see Nominatim suggestions -> click suggestion -> see nearby stops -> click stop to select -> appears in My stops -> reload page -> My stops still there
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/kollektivt && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>App renders correctly, all components wired together, full search-select-persist flow functional, build succeeds</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Verify complete search and selection flow</name>
  <files>n/a</files>
  <action>
Human verifies the complete Phase 1 flow in browser.

What was built: Complete search and stop selection flow — address search with Nominatim autocomplete, geolocation button, nearby stops list, stop selection with localStorage persistence.

How to verify:
1. Run `npm run dev` in the project directory
2. Open the URL shown (usually http://localhost:5173)
3. First visit: verify you see "Kollektivt" heading, search field with instruction text
4. Type "vegagatan stockholm" — after a brief pause, verify Nominatim suggestions appear in dropdown
5. Click a suggestion — verify nearby stops appear sorted by distance (e.g. "350m")
6. Click a stop to select it — verify it appears in "My stops" section at top
7. Select 2-3 stops, then reload the page — verify "My stops" persists
8. Click a stop in "My stops" to deselect — verify it disappears
9. Click "Use my location" button (allow browser permission) — verify nearby stops appear
10. Verify search field stays visible at all times, even after selecting stops
  </action>
  <verify>
    <automated>cd /Users/richardsandenskog/Claude/kollektivt && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>User approves the flow — type "approved" or describe issues to fix</done>
</task>

</tasks>

<verification>
1. `npm run build` completes without errors
2. `npm run dev` serves the app on localhost
3. Full flow: search -> select -> persist -> reload -> persisted
4. Geolocation flow: click button -> nearby stops appear
5. localStorage contains `kollektivt_data` with version field after selecting a stop
</verification>

<success_criteria>
- User can type an address and see Nominatim suggestions in dropdown
- User can click a suggestion and see up to 10 nearby stops within 1km, sorted by distance
- User can use "Use my location" for geolocation-based stop discovery
- User can select/deselect stops by tapping them
- Selected stops appear in "My stops" section above search
- Selections persist across page reload via localStorage
- First-time users see instruction text, returning users see their saved stops
- Search field always visible
</success_criteria>

<output>
After completion, create `.planning/phases/01-search-and-stop-selection/01-02-SUMMARY.md`
</output>
